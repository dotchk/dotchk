---
import SingleCommandBlock from './SingleCommandBlock.astro';
---

<section id="commands" class="section">
	<h2>Commands</h2>
	
	<div class="commands-grid">
		<div class="command-card">
			<h3>Direct Check</h3>
			<p>Direct domain availability checking</p>
			<SingleCommandBlock command="dotchk domain.com domain.net" />
			<div class="command-options">
				<h4>Options:</h4>
				<ul>
					<li><code>--parallel</code> - Number of parallel checks (default: 100)</li>
					<li><code>--timeout</code> - DNS query timeout (default: 500ms)</li>
					<li><code>--quiet</code> - Minimal output</li>
				</ul>
			</div>
		</div>

		<div class="command-card">
			<h3>pattern</h3>
			<p>Pattern-based domain search (shows available domains only)</p>
			<SingleCommandBlock command='dotchk pattern "[a-z]{4}.com" --limit 100' />
			<div class="command-options">
				<h4>Pattern Syntax:</h4>
				<ul>
					<li><code>[a-z]</code> - Any lowercase letter</li>
					<li><code>[0-9]</code> - Any digit</li>
					<li><code>{3}</code> - Exactly 3 repetitions</li>
					<li><code>(a|b|c)</code> - One of the options</li>
				</ul>
				<p class="note">Note: Only displays available domains for easier discovery</p>
			</div>
		</div>

		<div class="command-card">
			<h3>bulk</h3>
			<p>Bulk file checking</p>
			<SingleCommandBlock command="dotchk bulk domains.txt --export results.csv" />
			<div class="command-options">
				<h4>Options:</h4>
				<ul>
					<li><code>--stats</code> - Show summary statistics</li>
					<li><code>--export</code> - Export results to file</li>
					<li><code>--available-only</code> - Show only available</li>
				</ul>
			</div>
		</div>

		<div class="command-card">
			<h3>tld</h3>
			<p>Multi-TLD search</p>
			<SingleCommandBlock command="dotchk tld mybrand --popular" />
			<div class="command-options">
				<h4>Options:</h4>
				<ul>
					<li><code>--popular</code> - Popular TLDs (com, net, org, io, dev, etc.)</li>
					<li><code>--tech</code> - Technology TLDs (io, dev, app, tech, cloud, ai, etc.)</li>
					<li><code>--business</code> - Business TLDs (com, biz, company, inc, etc.)</li>
					<li><code>--creative</code> - Creative TLDs (design, studio, art, media, etc.)</li>
					<li><code>--retail</code> - E-commerce TLDs (shop, store, buy, sale, etc.)</li>
					<li><code>--country</code> - Popular country codes (us, uk, de, fr, etc.)</li>
					<li><code>--all</code> - Check all 1,080+ public TLDs</li>
					<li><code>--tlds</code> - Custom TLD list</li>
				</ul>
			</div>
		</div>
	</div>
</section>

<style>
	.commands-grid {
		display: grid;
		grid-template-columns: repeat(2, 1fr);
		gap: 2rem;
	}

	@media (max-width: 968px) {
		.commands-grid {
			grid-template-columns: 1fr;
		}
	}

	.command-card {
		background: rgba(255, 255, 255, 0.02);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 12px;
		padding: 2rem;
		transition: all 0.3s;
		position: relative;
		overflow: hidden;
	}

	.command-card::before {
		content: '';
		position: absolute;
		top: 0;
		left: 0;
		right: 0;
		height: 3px;
		background: linear-gradient(90deg, var(--color-accent), #00ffff);
		transform: translateX(-100%);
		transition: transform 0.3s;
	}

	.command-card:hover::before {
		transform: translateX(0);
	}

	.command-card:hover {
		background: rgba(255, 255, 255, 0.04);
		border-color: rgba(0, 255, 136, 0.2);
		transform: translateY(-2px);
	}

	.command-card h3 {
		font-size: 1.75rem;
		margin-top: 0;
		margin-bottom: 0.5rem;
		color: var(--color-accent);
		font-family: var(--font-mono);
	}

	.command-card > p {
		margin-bottom: 1.5rem;
		color: var(--color-text-dim);
	}

	.command-options {
		margin-top: 1.5rem;
	}

	.command-options h4 {
		font-size: 0.875rem;
		text-transform: uppercase;
		letter-spacing: 0.1em;
		color: var(--color-text-dim);
		margin-bottom: 0.5rem;
	}

	.command-options ul {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.command-options li {
		padding: 0.25rem 0;
		font-size: 0.875rem;
		color: var(--color-text-dim);
	}

	.command-options code {
		color: var(--color-accent);
		background: rgba(0, 255, 136, 0.1);
	}

	.command-options .note {
		margin-top: 1rem;
		padding: 0.75rem 1rem;
		background: rgba(0, 255, 136, 0.05);
		border-left: 3px solid var(--color-accent);
		border-radius: 4px;
		font-size: 0.875rem;
		color: var(--color-text-dim);
	}
</style>