---
import CommandBlock from './CommandBlock.astro';
---

<section id="performance" class="section">
	<h2>Performance</h2>
	
	<div class="perf-grid">
		<div class="perf-card">
			<div class="perf-icon">
				<img src="/assets/images/logo-lightning-fast.png" alt="Lightning Fast" width="60" height="60" loading="lazy">
			</div>
			<h3>Lightning Fast</h3>
			<p>Process 10,000+ domains per second with optimized DNS queries and smart caching.</p>
		</div>

		<div class="perf-card">
			<div class="perf-icon">
				<img src="/assets/images/logo-concurrent-processing.png" alt="Concurrent Processing" width="60" height="60" loading="lazy">
			</div>
			<h3>Concurrent Processing</h3>
			<p>Configurable parallel connections for maximum throughput on bulk operations.</p>
		</div>

		<div class="perf-card">
			<div class="perf-icon">
				<img src="/assets/images/logo-smart-caching.png" alt="Smart Caching" width="60" height="60" loading="lazy">
			</div>
			<h3>Smart Caching</h3>
			<p>Intelligent DNS response caching reduces redundant queries and improves speed.</p>
		</div>
	</div>

	<div class="perf-example">
		<h3>Optimize for Speed</h3>
		<CommandBlock code={`# Increase concurrency for bulk operations
$ dotchk bulk huge-list.txt --concurrent 200

# Use pattern limits for faster results
$ dotchk pattern "[a-z]{5}.com" --limit 1000 --concurrent 100`} />
	</div>

	<div class="warning">
		<h3>Important Note</h3>
		<p>dotchk uses NS record queries for maximum speed. This means:</p>
		<ul>
			<li>Results indicate nameserver presence, not registration status</li>
			<li>Some registered domains may show as available if they lack nameservers</li>
			<li>Always verify with WHOIS before purchasing any domain</li>
			<li>Respect rate limits and DNS server policies</li>
		</ul>
	</div>
</section>

<style>
	.perf-grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
		gap: 2rem;
		margin-bottom: 3rem;
	}

	.perf-card {
		text-align: center;
		padding: 2rem;
		background: rgba(255, 255, 255, 0.02);
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 12px;
		transition: all 0.3s;
	}

	.perf-card:hover {
		background: rgba(255, 255, 255, 0.04);
		border-color: rgba(0, 255, 136, 0.2);
		transform: translateY(-2px);
	}

	.perf-icon {
		margin-bottom: 1rem;
		height: 80px;
		display: flex;
		align-items: center;
		justify-content: center;
	}

	.perf-icon img {
		width: 60px;
		height: 60px;
		filter: brightness(1.2);
		transition: all 0.3s;
	}

	.perf-card:hover .perf-icon img {
		transform: scale(1.1);
		filter: brightness(1.4) drop-shadow(0 0 20px rgba(0, 255, 136, 0.6));
	}

	.perf-card h3 {
		font-size: 1.25rem;
		margin-bottom: 0.5rem;
		color: var(--color-accent);
	}

	.perf-card p {
		margin-bottom: 0;
		color: var(--color-text-dim);
	}

	.perf-example {
		background: rgba(0, 255, 136, 0.05);
		border: 1px solid rgba(0, 255, 136, 0.2);
		border-radius: 12px;
		padding: 2rem;
		margin-bottom: 2rem;
	}

	.perf-example h3 {
		margin-top: 0;
		color: var(--color-accent);
	}

	.perf-example pre {
		margin-bottom: 0;
		background: rgba(0, 0, 0, 0.3);
	}

	.warning {
		background: rgba(255, 200, 0, 0.05);
		border: 1px solid rgba(255, 200, 0, 0.2);
		border-radius: 12px;
		padding: 2rem;
	}

	.warning h3 {
		margin-top: 0;
		color: #ffc800;
	}

	.warning ul {
		list-style: none;
		padding: 0;
		margin: 0;
	}

	.warning li {
		padding: 0.5rem 0;
		padding-left: 1.5rem;
		position: relative;
		color: var(--color-text-dim);
	}

	.warning li::before {
		content: '•';
		position: absolute;
		left: 0;
		color: #ffc800;
		font-weight: bold;
	}

	.comment {
		color: #666;
	}
</style>